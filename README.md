# instagram-clone
instagram-clone. using react and firebase

//architecture
// src
// => components
// => constants
// => hooks
// => services (firebase)
// => styles (tailwind)
// =? store (zustand)
